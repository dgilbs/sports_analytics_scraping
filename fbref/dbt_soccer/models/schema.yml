
version: 2db

models:
  - name: player_goals
    description: "test"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
      - name: player
      - name: goals
